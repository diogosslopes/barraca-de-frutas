import './components.css';
import figures from '../images/Mask.svg'

function Header() {
    return (
      <header className="header">
            <img src={figures} />
      </header>
    );
  }
  
  export default Header;
  